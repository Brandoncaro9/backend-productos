package com.eafit.productoapi;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ProductoApiApplicationTests {

    @Test
    void contextLoads() {
        // Verifica que el contexto de Spring se levante correctamente,
        // es decir, que todos los beans (controller, service, repository)
        // esten bien conectados entre si.
    }
}
